# Macbook Automated Setup

This repo contains the set of scripts used to install the dependencies and configuration for the tools I use.

For convenience, all that has to be done to setup the machine state is to clone down the repo and run `./install.sh`.
